{% extends "base.twig" %}

{% block content %}
    {% for resource in resources %}
        <div class="content-section">
            <a href="{{resource.link}}">
                <h3>{{resource.name}}</h3>
                <p>{{resource.excerpt}}</p>
            </a>
        </div>
    {% endfor %}
{% endblock %}
